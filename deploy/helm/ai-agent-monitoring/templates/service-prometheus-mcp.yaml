apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-agent-monitoring.fullname" . }}-prometheus-mcp
  labels:
    {{- include "ai-agent-monitoring.labels" . | nindent 4 }}
    {{- include "ai-agent-monitoring.selectorLabels" (dict "context" . "component" "prometheus-mcp") | nindent 4 }}
spec:
  type: {{ .Values.prometheusMcp.service.type }}
  ports:
    - port: {{ .Values.prometheusMcp.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-agent-monitoring.selectorLabels" (dict "context" . "component" "prometheus-mcp") | nindent 4 }}
