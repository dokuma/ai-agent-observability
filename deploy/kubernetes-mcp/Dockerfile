FROM ghcr.io/containers/kubernetes-mcp-server:latest
# Base image: ENTRYPOINT ["/app/kubernetes-mcp-server"] CMD ["--port", "8080"]
# docker-compose.yaml の command で引数を上書きする。
# ENTRYPOINT はオーバーライドしない（バイナリパスが /app/ 配下のため）。
